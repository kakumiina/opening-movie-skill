---
name: opening-movie
description: 写真と文字がテンポよく切り替わるオープニング映像を、動画編集ソフトなしで作る。HyperFrames（HTML→MP4）で組み、素材画像は画像生成AI、BGMは合成スクリプトで用意し、check→renderまで通す。トリガー「オープニング動画作って」「opening-movie」「三役紹介の動画」「スライドショー動画作って」。
---

# opening-movie

人物写真・役職・名前・スローガンが順に出るオープニング映像を、**動画編集ソフトを使わずに**作るパイプライン。10秒でも20秒でも同じ型で作れる。

想定される依頼: 団体や会社の期初オープニング、新体制の紹介、イベントの冒頭映像、サービス紹介の導入。

## 完成イメージ

- 1920x1080 / 音声つき / MP4
- 濃紺のデジタル背景（グリッド・光の筋・粒子）に、人物が斜めのマスクで左に入り、右に役職と名前
- シーンの切り替わりは音楽の小節頭に一致

尺の目安と割り付け（BPM120＝1小節2秒に揃える）:

- 10秒版 … タイトル2秒 / 人物3名×2秒 / スローガン2秒
- 20秒版 … タイトル4秒 / 人物3名×4秒 / スローガン4秒

## 前提

- Node.js 22以上
- FFmpeg
- 画像生成の手段（codex / Midjourney / ChatGPT など何でもよい）
- 日本語フォント（macOS同梱のNoto Sans CJK JPやヒラギノでよい）

`npx hyperframes doctor` が全部緑になってから着手する。

## 手順

### 1. 素材画像を作る

背景1枚＋人物の枚数分。**画像には文字を焼き込まない**（誤字が出る／差し替えできなくなる）。文字は動画側で乗せる。

指示のテンプレート:

```
16:9の画像を N 枚作って。文字とロゴは一切入れないこと。
1) デジタル感のある抽象背景（濃紺、赤い光の筋、奥行きのあるグリッド、粒子）
2) 〈人物の属性〉のバストアップ、〈服装〉、濃紺の背景、
   被写体はやや左寄せで右側に余白
（以降 同じトーンで人数分）
```

守ること:

- 人物は「バストアップ・やや左寄せ・右に余白」で統一する。文字を置く場所を空けるため
- 背景の色調を全カットで揃える。素材がバラバラでも一本の映像に見える
- **1枚ずつ順番に生成する**。並列で走らせると出力を取り違える（生成物が共通フォルダに落ちるツールの場合）

### 2. BGMを用意する

著作権フリー音源を用意するのが早い。無ければ同梱の合成スクリプトを使う。

```bash
node scripts/make_bgm.mjs --duration 20 --bpm 120 --out assets/bgm.wav
ffmpeg -y -i assets/bgm.wav -c:a aac -b:a 192k assets/bgm.m4a
```

**最重要のコツ**: BPMを決めて、シーンの切り替わりを小節の頭に合わせる。4拍子のBPM120なら1小節=2秒なので、切替は2秒の倍数に置く。ここがズレると、絵が綺麗でも素人くさく見える。

### 3. HTMLで組み立てる

```bash
npx --yes hyperframes init <project> --non-interactive --example blank
cd <project> && mkdir -p assets
```

`templates/opening.html` を `index.html` としてコピーし、次を書き換える:

- 団体名・英字表記・期の表記
- 人物ごとの役職（英字/日本語）と氏名
- スローガン
- 各シーンの `data-start` / `data-duration`
- 画像と音声のファイル名

`--non-interactive` のときは `--example` の指定が必須。

### 4. 検査 → 書き出し

```bash
npm run check   # 緑になるまでrenderしない
npm run render
```

`check` はレイアウト・遮蔽・コントラスト・動きをまとめて見る。参考値として1920x1080の20秒がM4 Mac miniで23.4秒で書き出せる。

### 5. 公開（任意）

ブラウザ経由でアップロードする場合、**エージェントが渡せるファイルは10MB程度が上限**のことが多い（YouTube自体の上限ではない）。先に圧縮する。

```bash
ffmpeg -i in.mp4 -c:v libx264 -crf 22 -preset slow -pix_fmt yuv420p \
       -c:a aac -b:a 160k out.mp4
```

公開状態は画面の見た目で判断せず、外部から取得して確認する。

## 守らないと動かないルール（HyperFramesの決定論制約）

1コマずつ時刻を指定して描画するため、「同じ時刻なら必ず同じ絵」が絶対条件になる。

- `Math.random()` をそのまま使わない。粒子の配置などは**シード固定の疑似乱数**を同期処理で回す
- `Date.now()` `performance.now()` `setTimeout` を使わない
- 無限ループ（`repeat: -1`）を使わない。`Math.max(0, Math.floor(全体尺 / 1周) - 1)` で有限回にする
- `class="clip"` が付いた要素そのものをアニメーションさせない。**中に入れたラッパーを動かす**
- 退場アニメの後には `tl.set(セレクタ, { opacity: 0 }, 退場完了時刻)` を必ず添える
- タイムラインは同期的に作り、`window.__timelines["<composition-id>"]` に登録する

## checkでよく出る指摘と対処

- `gsap_exit_missing_hard_kill` … 退場後に `tl.set(..., { opacity: 0 }, 時刻)` を足す
- `text_occluded` … 覆っている装飾（ビネット等）を背景レイヤー側へ下げる。中心が透明でも検査器は不透明として扱う
- `panel_out_of_canvas` / `escaped_container` … 画面外へ流れる演出なら、動く層に `data-layout-allow-overflow` を付ける
- `system_font_will_alias` … 置換先のフォント名を直接書く
- コントラスト不足 … 文字色の不透明度を上げる（HUD等の細い文字が引っかかりやすい）

## 差し替え

- 写真 … `assets/` の画像を同じ名前で上書き
- 名前・役職 … `index.html` のテキストを書き換え
- 尺の変更 … ルートの `data-duration` と各シーンの `data-start` / `data-duration` を等倍にし、GSAPの時刻も同じ倍率でずらす。BGMも同じ尺で作り直す
- 顔の位置 … `.portrait img` の `object-position` を調整（例 `42% 32%`）

## つまずきやすい6点

- 画像に文字を焼き込む → 誤字が出て差し替え不能になる
- 画像生成を並列実行 → 出力の取り違え
- シーン切替を音楽に合わせない → 素人くさい仕上がり
- checkを飛ばしてrender → 崩れた動画ができてから気づく
- アップロードのファイルサイズ上限 → 事前に圧縮する
- 公開状態をスクリーンショットで判断 → 外部から取得して裏を取る

## 所要時間の目安

素材生成5〜8分 / BGM 1〜5分 / HTML 10〜20分 / check修正5〜10分 / render 15〜25秒 / 公開3分。初回30〜45分、2本目以降は10分程度。
